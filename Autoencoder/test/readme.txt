This is the other version related to the AGN MNA and VAE.

In this version,change the architecture of the model.Add
some funtion to increase the complexity of the code.

The direction "CLASS" include the class define of the 
AGN MNA and VAE.You can use it to the other project direction.

The direction "MNIST_data" is the mnist datasets.It include the 
train and the test datasets.

The file "AGN_Autocoder.py" is the whole code of the AGN.If you
run the code directly, you will get some outputs which include
the origial pictures the hidden layer pictures and the reconstraction
picture.It also plot the cruve of the cost.You can choose the suit
optimizer.But some optimizer will get the "nan", You can change the 
other optimizer.

The file "MNA_Autocoder.py" is the whole code of the MNA.

The file "VAE_Autocoder.py" is the whole code of the VAE.

The file "test.ipython" include the whole code.You can try it
and change it.

I hope you will enjoy it!!!

Email:1575262785@qq.com
Creator: √Œ‘⁄∫Œ∑Ω°£